[Unit]
Description=Conjur
After=docker.service
Requires=docker.service

[Service]
TimeoutStartSec=0
ExecStart=/usr/bin/docker start -a conjur
ExecStop=-/usr/bin/docker stop conjur

[Install]
WantedBy=multi-user.target
