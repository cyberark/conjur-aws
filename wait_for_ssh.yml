- debug: 
    msg: "ssh host: {{ ssh_host }}"

- name: Wait for SSH to come up
  wait_for:
    host: "{{ ssh_host }}"
    port: 22
    delay: 60
    timeout: 320
    state: started
  register: result
  retries: 5
  delay: 0
  until: result|success
